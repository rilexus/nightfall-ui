const basename = "/nightfall-ui";

const production = process.env.production;
const development = process.env.development;
const repoURL = "https://github.com/rilexus/nightfall-ui";

export { basename, production, development, repoURL };
