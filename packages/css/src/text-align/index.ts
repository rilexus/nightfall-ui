import { css } from "styled-components";

const textLeft = css`
  text-align: left;
`;

const textRight = css`
  text-align: right;
`;

const textCenter = css`
  text-align: center;
`;

const textJustify = css`
  text-align: justify;
`;

export { textCenter, textJustify, textLeft, textRight };
