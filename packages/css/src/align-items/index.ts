import { css } from "styled-components";

const itemsStart = css`
  align-items: flex-start;
`;

const itemsEnd = css`
  align-items: flex-end;
`;

const itemsCenter = css`
  align-items: center;
`;

const itemsBaseline = css`
  align-items: baseline;
`;
const itemsStretch = css`
  align-items: stretch;
`;

export { itemsBaseline, itemsCenter, itemsEnd, itemsStart, itemsStretch };
