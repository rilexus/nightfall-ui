import { css } from "styled-components";

const block = css`
  display: block;
`;

const inlineBlock = css`
  display: inline-block;
`;

const inline = css`
  display: inline;
`;
const flex = css`
  display: flex;
`;
const inlineFlex = css`
  display: inline-flex;
`;

export { block, inline, inlineBlock, inlineFlex, flex };
